export enum FeatureKey {
  Employees = "employees",
  Game = "game",
  Navigation = "navigation",
  Auth = "auth",
  Leaderboard = "leaderboard",
  I18n = "i18n",
  Lottery = "lottery",
  Admin = "admin",
}

export enum AsyncStatus {
  Idle = "idle",
  Loading = "loading",
  Succeeded = "succeeded",
  Failed = "failed",
}
