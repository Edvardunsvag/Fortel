export enum FeatureKey {
  Employees = 'employees',
  Game = 'game',
  Settings = 'settings',
}

export enum AsyncStatus {
  Idle = 'idle',
  Loading = 'loading',
  Succeeded = 'succeeded',
  Failed = 'failed',
}

