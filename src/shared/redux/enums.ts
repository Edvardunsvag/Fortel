export enum FeatureKey {
  Employees = 'employees',
  Game = 'game',
  Settings = 'settings',
  Navigation = 'navigation',
}

export enum AsyncStatus {
  Idle = 'idle',
  Loading = 'loading',
  Succeeded = 'succeeded',
  Failed = 'failed',
}

