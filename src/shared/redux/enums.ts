export enum FeatureKey {
  Employees = 'employees',
  Game = 'game',
  Navigation = 'navigation',
  Auth = 'auth',
  Leaderboard = 'leaderboard',
}

export enum AsyncStatus {
  Idle = 'idle',
  Loading = 'loading',
  Succeeded = 'succeeded',
  Failed = 'failed',
}

